{
  "name": "@radicalcondor/tsconfig",
  "description": "TypeScript config for Radical Condor repositories",
  "version": "0.1.11",
  "license": "MIT",
  "main": "tsconfig.json",
  "keywords": [
    "radicalcondor",
    "tsconfig"
  ],
  "author": {
    "name": "Radical Condor",
    "url": "https://github.com/radicalcondor"
  },
  "homepage": "https://github.com/radicalcondor/config",
  "repository": {
    "type": "git",
    "url": "https://github.com/radicalcondor/config.git",
    "directory": "packages/tsconfig"
  },
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "precommit": "lint-staged",
    "pretest": "curl https://json.schemastore.org/tsconfig > schema.json",
    "test": "ajv validate -s schema.json -d tsconfig.json --errors=json",
    "posttest": "rimraf schema.json",
    "build": "echo '0'",
    "lint": "yarn test"
  },
  "peerDependencies": {
    "typescript": ">=4"
  },
  "devDependencies": {
    "@radicalcondor/lintstaged-config": "^0.1.11",
    "@radicalcondor/prettier-config": "^0.1.11",
    "ajv-cli": "^3",
    "lint-staged": "^10",
    "prettier": "^2",
    "rimraf": "^3"
  },
  "publishConfig": {
    "access": "public"
  }
}
